<!DOCTYPE html>
<html>
<head>
    <title>Area Calculator - <%= data.shape %></title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <h1>Calculate Area of a <%= data.shape %></h1>

    <% if (data.area) { %>
        <p>The area of the <%= data.shape %> is: <%= data.area %></p>
    <% } else { %>
        <form action="/calculate/rectangle" method="post">
            <h3>Length:</h3> <input type="text" name="length" required>
            <h3>Breadth:</h3> <input type="text" name="width" required>
            <button type="submit">Calculate Area</button>
        </form>

        <h3>Formula: <%= data.formula %></h3>
        <img src="<%= data.imagePath %>" alt="<%= data.shape %>">
    <% } %>
</body>
</html>
