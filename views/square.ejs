<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="styles.css">
    <title>Area Calculator - <%= data.shape %></title>
</head>
<body>
    <h1 class="head">Calculate Area of a <%= data.shape %></h1>

    <% if (data.area) { %>
        <p class="para">The area of the <%= data.shape %> is: <%= data.area %></p>
    <% } else { %>
        <form action="/calculate/square" method="post">
            <h3 class="h31">Side:</h3> <input type="text" name="sideLength" required>
            <button type="submit">Calculate Area</button>
        </form>

        <h3>Formula: <%= data.formula %></h3>
        <img src="<%= data.imagePath %>" alt="<%= data.shape %>">
    <% } %>



</body>
</html>
